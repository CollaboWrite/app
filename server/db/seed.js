const testSeed = require('./testSeed')

const firebase = require('./index')
const db = firebase.database()

// TO SEED, un-comment and run this file
// const testSeedDb = db.ref('testProjects').set(testSeed)
